<% className = form_action.delete(:class) %>
<%= form_tag(form_action, :id => "#{form_type}_form", :enctype => "multipart/form-data", :class => className) do %>

  <%= render :partial => "form" %>

<% end %>
<% if @article.id && current_user.admin? %>
	<%= form_tag("/admin/content/merge") do %>
		<h4><%= _("Merge Articles") %></h4>
		<label for="merge_with" style="width:auto;margin:auto">Article ID</label>
		<input type="text" name="merge_with" style="margin:auto"/>
		<input type="hidden" name="id" value=<%= @article %> />
		<%= submit_tag 'Merge', {:class => 'btn info'} %>
	<% end %>
<% end %>
